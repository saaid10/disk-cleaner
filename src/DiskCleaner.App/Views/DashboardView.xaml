<UserControl x:Class="DiskCleaner.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DiskCleaner.App.ViewModels"
             xmlns:conv="clr-namespace:DiskCleaner.App.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:FractionToStarConverter x:Key="FractionToStarConverter"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button DockPanel.Dock="Right" Content="Refresh" Padding="12,4" Command="{Binding RefreshCommand}"/>
            <TextBlock Text="Dashboard" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Drives}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:DriveCardViewModel}">
                    <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="{Binding VolumeLabel}" FontWeight="Bold" FontSize="14"/>
                            <TextBlock FontSize="12" Foreground="#666666">
                                <Run Text="{Binding Name, Mode=OneWay}"/>
                            </TextBlock>

                            <Grid Height="14" Margin="0,8,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding UsedFraction, Converter={StaticResource FractionToStarConverter}}"/>
                                    <ColumnDefinition Width="{Binding FreeFraction, Converter={StaticResource FractionToStarConverter}}"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#4C8BF5" CornerRadius="4,0,0,4"/>
                                <Border Grid.Column="1" Background="#E5E5E5" CornerRadius="0,4,4,0"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Used: "/>
                                <TextBlock Text="{Binding UsedDisplay}" FontWeight="SemiBold"/>
                                <TextBlock Text="  |  Free: " Margin="12,0,0,0"/>
                                <TextBlock Text="{Binding FreeDisplay}" FontWeight="SemiBold"/>
                                <TextBlock Text="  |  Total: " Margin="12,0,0,0"/>
                                <TextBlock Text="{Binding TotalDisplay}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
