<UserControl x:Class="DiskCleaner.App.Views.TreemapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Disk Scan" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="{Binding DriveSpaceSummary}" Foreground="#666666" Margin="0,0,0,12"/>

        <DockPanel Grid.Row="2" Margin="0,0,0,10">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Up" Padding="10,4" Margin="0,0,8,0" Command="{Binding NavigateUpCommand}"/>
                <Button Content="Refresh" Padding="10,4" Command="{Binding RefreshCommand}"/>
            </StackPanel>
            <TextBlock Text="{Binding CurrentPath}" VerticalAlignment="Center" FontFamily="Consolas" TextTrimming="CharacterEllipsis"/>
        </DockPanel>

        <Grid Grid.Row="3">
        <Border BorderBrush="#CCCCCC" BorderThickness="1">
            <ItemsControl ItemsSource="{Binding Rects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="760" Height="460"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="{Binding Width}" Height="{Binding Height}"
                                Background="{Binding Category, Converter={StaticResource FileTypeCategoryToColorConverter}}"
                                BorderBrush="White" BorderThickness="1"
                                Command="{Binding DataContext.NavigateIntoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                ToolTip="{Binding Label}">
                            <TextBlock Text="{Binding Label}" Foreground="White" FontSize="10"
                                       TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"
                                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
        <TextBlock Text="Loading..." FontSize="14" Foreground="#666666"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
