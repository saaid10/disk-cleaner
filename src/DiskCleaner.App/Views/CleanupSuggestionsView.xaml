<UserControl x:Class="DiskCleaner.App.Views.CleanupSuggestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right" Content="Scan" Padding="12,4" Command="{Binding ScanCommand}"/>
            <TextBlock Text="Cleanup Suggestions" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Select All" Padding="10,4" Margin="0,0,8,0" Command="{Binding SelectAllCommand}"/>
            <Button Content="Select None" Padding="10,4" Margin="0,0,16,0" Command="{Binding SelectNoneCommand}"/>
            <Button Content="Delete Selected" Padding="10,4" Command="{Binding DeleteSelectedCommand}"
                    Background="#FBE3E1"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False"
                  CanUserAddRows="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="34"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="3*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="1.2*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Downloaded" Binding="{Binding LastModifiedDisplay}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Auto-clean eligible" Binding="{Binding WhitelistEligible}" Width="Auto" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Margin="0,10,0,0" Foreground="#555555"/>
    </Grid>
</UserControl>
