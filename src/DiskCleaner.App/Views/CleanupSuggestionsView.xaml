<UserControl x:Class="DiskCleaner.App.Views.CleanupSuggestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button DockPanel.Dock="Right" Content="Rescan" Padding="12,4" Command="{Binding ScanCommand}"/>
            <TextBlock Text="Cleanup Suggestions" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="3*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="1.2*"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="Auto"/>
                <DataGridTextColumn Header="Auto-clean eligible" Binding="{Binding WhitelistEligible}" Width="Auto"/>
                <DataGridTemplateColumn Header="" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding IsQuarantined, Converter={StaticResource BoolToQuarantineLabelConverter}}"
                                    Command="{Binding QuarantineCommand}"
                                    IsEnabled="{Binding IsQuarantined, Converter={StaticResource InverseBoolConverter}}"
                                    Padding="8,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Margin="0,10,0,0" Foreground="#555555"/>
    </Grid>
</UserControl>
