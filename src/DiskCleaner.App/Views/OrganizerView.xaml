<UserControl x:Class="DiskCleaner.App.Views.OrganizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel Margin="16">
        <TextBlock Text="Desktop Organizer" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
        <TextBlock Margin="0,0,0,12">
            <Run Text="Sorts loose files sitting directly on: "/>
            <Run Text="{Binding DesktopPath, Mode=OneWay}" FontWeight="SemiBold"/>
        </TextBlock>
        <TextBlock Foreground="#666666" TextWrapping="Wrap" Margin="0,0,0,16">
            Existing subfolders are left alone. Files are grouped by type (Documents, Images,
            Archives, Videos, Audio, Programs, Other), with game shortcuts and installers
            (Steam/Epic/GOG/Battle.net/Origin) routed to a Games folder instead.
        </TextBlock>

        <StackPanel Orientation="Horizontal">
            <Button Content="Organize Now" Padding="14,6" Margin="0,0,10,0" Command="{Binding OrganizeNowCommand}"/>
            <Button Content="Undo Last Organize" Padding="14,6" Command="{Binding UndoLastCommand}" IsEnabled="{Binding CanUndo}"/>
        </StackPanel>

        <TextBlock Text="{Binding StatusText}" Margin="0,16,0,0" Foreground="#555555"/>
    </StackPanel>
</UserControl>
