<Window x:Class="DiskCleaner.App.Views.CleanupReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Cleanup Complete" Height="480" Width="560" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cleanup Complete" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="{Binding TotalReclaimedDisplay}" FontSize="15" Foreground="#2E7D32" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="By category" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <ItemsControl ItemsSource="{Binding CategoryRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="0,1">
                            <Run Text="{Binding CategoryName, Mode=OneWay}"/>
                            <Run Text=" - "/>
                            <Run Text="{Binding ItemCount, Mode=OneWay}"/>
                            <Run Text=" item(s), "/>
                            <Run Text="{Binding BytesDisplay, Mode=OneWay}"/>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Quarantined items (restorable for 7 days)" Margin="0,16,0,0">
            <ListBox ItemsSource="{Binding ItemRows}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="0,1">
                            <Run Text="{Binding Path, Mode=OneWay}"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding SizeDisplay, Mode=OneWay}"/>
                            <Run Text=")"/>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <Button Grid.Row="4" Content="OK" Padding="20,6" HorizontalAlignment="Right" Margin="0,16,0,0"
                IsDefault="True" Click="OnOkClicked"/>
    </Grid>
</Window>
