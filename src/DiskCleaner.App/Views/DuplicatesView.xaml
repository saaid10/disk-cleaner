<UserControl x:Class="DiskCleaner.App.Views.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DiskCleaner.App.ViewModels"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button DockPanel.Dock="Right" Content="Scan for Duplicates" Padding="12,4" Command="{Binding ScanCommand}"/>
            <TextBlock Text="Duplicates" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:DuplicateGroupRowViewModel}">
                        <Border BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock FontWeight="Bold">
                                    <Run Text="{Binding SizeEachDisplay, Mode=OneWay}"/>
                                    <Run Text=" each - wasting "/>
                                    <Run Text="{Binding WastedDisplay, Mode=OneWay}"/>
                                </TextBlock>
                                <ItemsControl ItemsSource="{Binding Files}" Margin="0,6,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:DuplicateFileRowViewModel}">
                                            <DockPanel Margin="0,2">
                                                <Button DockPanel.Dock="Right"
                                                        Content="{Binding IsQuarantined, Converter={StaticResource BoolToQuarantineLabelConverter}}"
                                                        IsEnabled="{Binding IsQuarantined, Converter={StaticResource InverseBoolConverter}}"
                                                        Command="{Binding QuarantineCommand}"
                                                        Padding="8,2"/>
                                                <TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis"/>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Margin="0,10,0,0" Foreground="#555555"/>
    </Grid>
</UserControl>
